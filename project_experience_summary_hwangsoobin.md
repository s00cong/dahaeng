# 프로젝트 경험 정리 - 황수빈

작성 목적: 입사지원서, 포트폴리오, 면접 준비에 활용할 수 있도록 프로젝트별 경험을 `개요`, `기술환경`, `담당 역할`, `성과 및 배운점`, `상세 내용` 기준으로 정리한다.

주의:

- 아래 내용은 제출용 초안이 아니라 소재 정리용 문서이다.
- 자기소개서 문항에 사용할 때는 한 문항에 한 프로젝트를 중심으로 작성하는 것이 좋다.
- 수치가 확인되지 않은 성과는 임의로 만들지 않는다.
- 다른 팀원 작성 커밋이 섞인 기능은 본인 기여 범위를 확인한 뒤 사용한다.

---

# 1. 다행

## 개요

다행은 여행 목표를 설정하고, 여행지 관련 데이터를 기반으로 예산과 항공권 정보를 제공하는 서비스이다.

프로젝트에서 항공권 데이터는 외부 사이트에서 직접 수집해야 했고, 수집된 데이터는 서비스 API와 알림 기능에서 사용할 수 있도록 정규화되어야 했다.  
이에 따라 Google Flight와 Trip.com 데이터를 수집하고, HDFS와 Spark ETL을 거쳐 DB에 적재하는 항공권 데이터 파이프라인을 구축·개선했다.

## 기술환경

- Backend: Java, Spring Boot
- Data Pipeline: Python, Spark, HDFS, JSONL
- Database: MongoDB, MariaDB 계열 저장소 사용 흔적 확인
- Infra/Runtime: Docker, Docker Compose, systemd timer/service
- Crawling/Automation: Google Flight, Trip.com, Playwright/headless 환경
- Collaboration: GitLab MR, Jira

## 담당 역할

- Google Flight, Trip.com 항공권 데이터 수집 로직 구현 및 개선
- 항공권 데이터 정규화, HDFS 업로드, Spark ETL, DB 적재 흐름 구성
- 항공권 수집 배치의 실패 구간 재처리 구조 개선
- 항공권 캘린더/추이/도시 요약 API 구현
- 항공권 알림 구독, 조회, 비활성화, 알림 목록, 읽음 처리 API 구현
- 가격 조건 판정 기반 항공권 알림 배치 구현
- 내부 토큰 기반 항공권 알림 배치 수동 실행 API 구현
- Docker 기반 항공권 배치 worker 실행 환경 구성
- 항공권 데이터 중복 적재, 수집일 기준, 누락 데이터 처리 기준 보정

## 성과 및 배운점

- 외부 데이터 수집부터 ETL, DB 적재, API, 알림까지 이어지는 데이터 흐름을 서비스 기능으로 연결했다.
- 항공권 수집 배치를 소스별 상태와 Step 단위 실행 계획으로 분리해, 전체 재실행이 아니라 실패 지점 이후만 재처리할 수 있도록 개선했다.
- 이미 성공한 크롤링·업로드 단계를 반복 실행하지 않고 실패 구간 중심으로 재처리할 수 있게 되어, 배치 운영 부담과 불필요한 반복 실행을 줄일 수 있었다.
- 외부 사이트 응답 지연, HDFS 통신 오류, 데이터 중복 적재처럼 운영 중 발생할 수 있는 문제를 고려해 배치 안정성을 개선하는 경험을 얻었다.
- API와 알림 배치가 같은 기준의 데이터를 사용하려면 ETL 단계의 데이터 정합성이 중요하다는 것을 배웠다.

## 상세 내용

### 항공권 데이터 파이프라인

Google Flight와 Trip.com에서 수집한 항공권 데이터를 서비스에서 사용할 수 있도록 JSONL, HDFS, Spark ETL, DB 적재 흐름으로 연결했다.  
단순히 데이터를 수집하는 것에 그치지 않고, 항공권 조회 API와 알림 배치에서 사용할 수 있는 형태로 데이터를 정규화했다.

활용 가능한 표현:

> Google Flight와 Trip.com 데이터를 수집하고, HDFS와 Spark ETL을 거쳐 항공권 조회 API와 알림 배치에서 사용할 수 있는 형태로 적재했습니다.

### 실패 구간 재처리 구조 개선

항공권 데이터 파이프라인은 크롤링, HDFS 업로드, Spark ETL, DB 적재로 이어졌다.  
외부 사이트 응답 지연이나 HDFS 통신 오류로 배치가 중간에 실패할 수 있었고, 전체 배치를 다시 실행하면 이미 성공한 단계까지 반복 실행되는 문제가 있었다.

이를 해결하기 위해 Google Flight와 Trip.com을 소스별로 분리하고, 마지막 성공 상태를 관리했다.  
또한 실행 과정을 Step 단위로 나누어 실패한 Step 이후부터 재처리할 수 있도록 구성했다.

활용 가능한 표현:

> 항공권 수집 배치를 소스별 상태와 Step 단위 실행 계획으로 분리해, 전체 배치 재실행이 아니라 실패 지점 이후만 재처리하도록 개선했습니다.

### 항공권 알림 백엔드

항공권 목표가 또는 근접가 조건에 따라 알림 후보를 계산하고, 사용자 구독 상태를 기반으로 알림을 제공하는 기능을 구현했다.  
알림 구독, 조회, 비활성화, 알림 목록, 읽음 처리 API와 가격 조건 판정 배치를 구성했다.

운영 편의성을 위해 내부 토큰 기반 수동 실행 API도 구현했다.  
이를 통해 정해진 스케줄을 기다리지 않고 항공권 알림 배치를 직접 실행할 수 있는 흐름을 만들었다.

활용 가능한 표현:

> 항공권 알림 구독 API와 가격 조건 판정 배치를 구현하고, 내부 토큰 기반 수동 실행 API를 통해 운영 중 배치를 즉시 실행할 수 있도록 구성했습니다.

### 데이터 정합성 개선

항공권 캘린더 데이터 중복 적재, 수집일 기준 불일치, 누락 데이터 처리 기준 문제를 보정했다.  
ETL 결과가 조회 API와 알림 배치에서 일관되게 사용될 수 있도록 데이터 기준을 정리했다.

활용 가능한 표현:

> 항공권 캘린더와 summary 데이터의 중복 적재, 기준일 불일치, 누락 데이터 처리 기준을 보정해 조회 API와 알림 배치에서 일관된 데이터를 사용할 수 있도록 개선했습니다.

### 항공권 알림 배치 RabbitMQ 기반 비동기 처리 개선

배치 성능 최적화 및 확장성 개선을 위해 항공권 알림 배치를 RabbitMQ 기반 메시지 큐 방식으로 재설계했다.

**개선 전:**
- 모든 구독을 동기로 순회하며 처리 → 구독 개수에 따라 배치 시간이 선형으로 증가
- 한 구독 처리 실패 시 이후 구독 처리에 영향
- 알림 생성과 이메일 발송이 같은 스레드에서 처리되어 이메일 지연 발생

**개선 후:**
- 배치에서는 모든 활성 구독에 대해 평가 메시지를 RabbitMQ 큐에 발행 (~1초)
- 별도의 컨슈머 워커(5-10개)가 각 구독을 병렬로 처리 → 전체 배치 시간 단축
- 알림 생성 후 이메일 발송 메시지를 별도 큐에 발행하여 완전히 분리
- 이메일 컨슈머(2-5개)가 비동기로 이메일 발송

**구현 내용:**
- RabbitMQ 설정: Direct Exchange, 두 개의 메인 큐(`flight-alert-eval`, `flight-alert-email`)와 Dead Letter Queue
- Message DTO: 구독 평가 메시지, 이메일 발송 메시지 정의
- Consumer: 각 큐별 비동기 리스너 구현
- Worker Service: 개별 구독 평가 및 이메일 발송 로직 분리
- Docker Compose: RabbitMQ 서비스 추가, 헬스체크 설정

**기술적 이점:**
- 배치 발행 시간 단축 (동기 처리 제거)
- 구독 평가의 병렬 처리로 전체 배치 시간 대폭 단축
- 한 구독 처리 실패가 다른 구독에 영향 없음 (장애 격리)
- 이메일 발송을 별도 워커에서 처리하여 알림 생성 지연 제거
- 메시지 재시도, Dead Letter Queue 등으로 안정성 향상
- 워커 수 조정으로 처리량 쉽게 제어 가능 (확장성)

활용 가능한 표현:

> 항공권 알림 배치를 RabbitMQ 기반 메시지 큐 방식으로 개선해, 구독 평가를 병렬 처리하고 이메일 발송을 분리했습니다. 이를 통해 배치 시간을 단축하고, 한 구독 실패가 다른 구독에 영향을 주지 않도록 장애를 격리했습니다.

또는:

> 항공권 알림 배치에 RabbitMQ를 도입해 동기 처리를 비동기 메시지 큐 방식으로 전환했습니다. 개별 구독을 5-10개 워커가 병렬 처리하고, 알림 생성과 이메일 발송을 완전히 분리하여 확장성과 안정성을 개선했습니다.

---

# 2. HearBe

## 개요

HearBe는 시각장애인 사용자와 보호자를 위한 서비스로, 인증, 복지카드 기반 회원가입, 주문 관리, 화면 공유 진입 흐름 등의 기능을 포함한다.

프로젝트에서 백엔드 담당 영역은 사용자 인증과 상태 관리, 민감정보 저장, 주문 도메인 설계, Redis 기반 임시 코드 관리 등 서비스의 기반 기능에 집중되어 있다.  
특히 Spring Security, JWT, Redis를 활용해 인증 흐름과 사용자 상태를 안정적으로 관리하는 경험을 쌓았다.

## 기술환경

- Backend: Java, Spring Boot
- Security: Spring Security, JWT, BCrypt
- Database/State: JPA, RDB, Redis
- Auth: Access Token, Refresh Token, HttpOnly Cookie
- Realtime/Session Entry: Redis TTL 기반 공유 코드, PeerJS 기반 화면 공유 진입 흐름
- Infra/Monitoring: Docker Compose, Prometheus, Grafana 사용 흔적 확인
- Collaboration: GitLab MR

## 담당 역할

- Spring Security와 JWT 기반 인증/인가 체계 구현
- JWT claim과 SecurityContext 기반 사용자 식별 구조 구성
- Redis TTL 기반 Refresh Token 저장 및 재발급 API 구현
- Refresh Token HttpOnly Cookie 전달 방식 적용
- 복지카드 기반 회원가입 기능 및 민감정보 AES 암호화 저장 구현
- 주문, 주문 상품, 플랫폼 관계 기반 주문 도메인 설계
- 다중 상품 주문 생성 및 주문 조회 API 구현
- 공통 예외 처리와 API 응답 표준화
- Redis TTL 기반 4자리 공유 코드 API 구현
- 화면 공유 기능에서 백엔드와 프론트엔드 역할 분리

## 성과 및 배운점

- 임시 사용자 식별 방식에서 벗어나, JWT와 SecurityContext 기반의 일관된 인증 흐름을 구성했다.
- Redis TTL을 활용해 Refresh Token과 공유 코드처럼 만료가 필요한 데이터를 관리했다.
- HttpOnly Cookie를 적용해 토큰 탈취 위험을 줄이고, 인증 상태를 더 안전하게 관리하는 방법을 경험했다.
- 민감정보를 다루는 기능에서는 저장 방식과 검증 흐름을 신중하게 설계해야 한다는 점을 배웠다.
- 실시간 화면 공유 기능에서는 백엔드가 모든 연결을 직접 처리하기보다, 서비스 요구사항과 일정에 맞춰 역할을 적절히 분리하는 것이 중요하다는 것을 배웠다.

## 상세 내용

### Spring Security + JWT 인증/인가

초기에는 일부 기능에서 사용자 식별값을 임시로 처리하거나, 인증된 사용자 정보를 일관되게 참조하지 못하는 문제가 있었다.  
사용자별 주문, 복지카드, 공유 코드 기능이 늘어나면서 공통 인증 기반이 필요하다고 판단했다.

Spring Security와 JWT를 도입해 로그인 후 Access Token을 발급하고, JWT claim의 사용자 식별값을 SecurityContext와 연동했다.  
컨트롤러와 서비스에서는 하드코딩된 사용자 ID를 제거하고, 공통 유틸을 통해 현재 인증된 사용자를 참조하도록 변경했다.

활용 가능한 표현:

> Spring Security와 JWT를 활용해 인증/인가 체계를 구성하고, JWT claim과 SecurityContext를 연동해 사용자 식별 로직을 일관되게 관리했습니다.

### Redis Refresh Token + HttpOnly Cookie

Access Token과 Refresh Token을 분리하고, Refresh Token은 Redis에 저장했다.  
Redis TTL을 적용해 만료 처리를 자동화했으며, Refresh Token을 HttpOnly Cookie로 전달해 JavaScript 접근에 의한 탈취 위험을 줄이고자 했다.

재발급 API에서는 토큰 타입과 Redis 저장값을 검증하도록 구성했다.

활용 가능한 표현:

> Refresh Token을 Redis에 TTL 기반으로 저장하고 HttpOnly Cookie로 전달해, 토큰 재발급 흐름과 만료 관리를 안정적으로 구성했습니다.

### 복지카드 기반 회원가입과 민감정보 암호화

시각장애인 사용자 유형의 회원가입 흐름을 구현하고, 복지카드 번호와 CVC 등 민감정보를 AES로 암호화해 저장했다.  
사용자 유형에 따라 가입 정책과 데이터 처리 방식이 달라지는 도메인 요구사항을 반영했다.

활용 가능한 표현:

> 복지카드 기반 회원가입 기능에서 민감정보를 AES로 암호화해 저장하고, 사용자 유형에 따른 가입 흐름을 구현했습니다.

### 주문 도메인 모델링

여러 외부 쇼핑 플랫폼의 주문 데이터를 통합 관리하기 위해 주문, 주문 상품, 플랫폼 관계를 설계했다.  
하나의 주문에 여러 상품이 포함되는 다중 상품 주문 구조를 구현하고, 주문 생성/조회 API를 구성했다.

활용 가능한 표현:

> 여러 외부 쇼핑 플랫폼의 주문 데이터를 통합 관리하기 위해 주문-주문상품-플랫폼 관계를 설계하고, 다중 상품 주문 API를 구현했습니다.

### Redis 공유 코드 API

시각장애인 사용자와 보호자가 화면 공유 세션에 진입할 수 있도록 4자리 공유 코드 API를 구현했다.  
공유 코드는 Redis에 저장하고 TTL을 적용해 일정 시간이 지나면 자동 만료되도록 구성했다.

화면 공유 자체는 PeerJS 기반 P2P 연결로 처리하고, 백엔드는 공유 코드 발급과 검증에 집중하도록 역할을 분리했다.

활용 가능한 표현:

> 실시간 화면 공유 진입을 위해 Redis TTL 기반 공유 코드 API를 구현하고, 백엔드는 세션 진입 검증에 집중하도록 역할을 분리했습니다.

---

# 3. 번역의 민족

## 개요

번역의 민족은 실시간 강의 통역과 슬라이드 번역을 제공하는 AI 기반 서비스이다.

프로젝트에서는 ASR, MT, TTS, VLM, OCR 등 여러 AI 처리 결과가 사용자에게 안정적으로 전달되도록 백엔드 파이프라인을 개선했다.  
특히 AI 모델 결과를 그대로 전달하지 않고 품질을 검증하거나, 장시간 작업의 진행 상태와 취소 처리를 관리하는 데 집중했다.

## 기술환경

- Backend: Python, FastAPI
- Realtime: WebSocket
- AI Pipeline: ASR, MT, TTS, VLM, OCR
- ASR: faster-whisper 기반 처리
- Text Processing: kss, 정규식 기반 fallback
- Document Processing: PDF 번역 파이프라인
- Frontend 연동: React 기반 화면과 상태 연동 흔적 확인

## 담당 역할

- ASR/MT 환각 출력 차단을 위한 품질 게이트 구현
- ASR 신뢰도 지표 기반 필터링 로직 구성
- MT 반복 출력 감지 및 후처리 로직 구성
- 검증 실패 결과가 MT/TTS 단계로 전파되지 않도록 차단
- 긴 ASR 결과를 문장 단위로 분할하고 WebSocket으로 순차 송출
- PDF/VLM 번역 작업의 취소 체크포인트와 리소스 정리 로직 구현
- 업로드 응답 전 취소 처리를 위한 `client_token` 기반 pending cancel 구조 구현
- 다중 PDF 배치 업로드에서 VLM 재사용 구조 개선
- 슬라이드 번역 단계별 진행률과 ETA 상태 모델 개선
- WebSocket 의도적 종료 후 자동 재연결 루프 방지

## 성과 및 배운점

- AI 모델 결과를 그대로 서비스에 전달하는 것이 아니라, 사용자에게 전달되기 전 검증하는 과정이 중요하다는 것을 배웠다.
- 무음 환각, 반복 번역, 낮은 신뢰도 결과가 자막이나 TTS로 전파되는 경로를 줄였다.
- 긴 발화를 문장 단위로 나누고 번역 완료 즉시 송출해, 첫 자막 지연을 줄일 수 있는 구조를 만들었다.
- 장시간 AI 작업에서 사용자의 취소 요청을 반영하고, 모델 리소스와 임시 파일을 정리하는 안정화 경험을 얻었다.
- 실시간 서비스에서는 정상 종료와 비정상 종료를 구분해 연결 상태를 관리하는 것이 중요하다는 것을 배웠다.

## 상세 내용

### ASR/MT 품질 게이트

실시간 통역 파이프라인에서 ASR 또는 MT 모델이 입력과 무관한 출력을 생성하는 문제가 있었다.  
무음이나 배경 소음만 있는 상황에서도 ASR이 실제 발화와 무관한 문장을 생성하거나, MT 모델이 짧은 입력에 대해 같은 단어를 반복 출력할 수 있었다.

이 결과가 그대로 WebSocket 자막이나 TTS로 전달되면 사용자는 실제 발화와 다른 내용을 받게 된다.  
따라서 단순 모델 품질 문제가 아니라 서비스 신뢰도 문제라고 판단했다.

이를 해결하기 위해 ASR 결과를 바로 후속 단계로 넘기지 않고, 백엔드 파이프라인에서 모델 출력의 신뢰도를 검증하는 품질 게이트를 추가했다.  
ASR 단계에서는 `compression_ratio`, `no_speech_prob`, `avg_logprob`를 기준으로 신뢰도 낮은 결과를 필터링했고, MT 단계에서는 반복 출력 감지와 문장 후처리를 적용했다.

활용 가능한 표현:

> 실시간 ASR/MT 파이프라인에서 모델 출력의 신뢰도를 검증하는 품질 게이트를 구현해, 무음 환각·반복 번역 결과가 자막/TTS까지 전파되는 문제를 줄였습니다.

### ASR 결과 문장 분할과 WebSocket 송출

긴 ASR 결과를 한 번에 번역하면 전체 문장 번역이 끝날 때까지 첫 자막이 표시되지 않는 문제가 있었다.  
사용자가 체감하는 지연을 줄이기 위해 ASR 결과를 문장 단위로 분리하고, 각 문장 번역이 완료되는 즉시 WebSocket으로 송출하도록 개선했다.

같은 ASR sequence 안에서 문장 순서를 관리하기 위해 `sub_seq`, `total_sub` 필드를 추가했다.

활용 가능한 표현:

> 긴 ASR 결과를 문장 단위로 분할하고, 문장별 MT 완료 즉시 WebSocket으로 송출하도록 개선해 첫 자막 출력 지연을 줄일 수 있는 구조를 만들었습니다.

### PDF/VLM 번역 작업 취소와 리소스 정리

PDF 슬라이드 번역은 OCR, VLM 번역, PDF 생성이 포함된 장시간 작업이었다.  
사용자가 업로드 중 또는 처리 중 취소해도 백그라운드 작업이 계속 실행되면 모델 리소스와 임시 파일이 남을 수 있었다.

업로드 응답 전 취소를 처리하기 위해 `client_token` 기반 pending cancel 구조를 도입했다.  
이미 처리 중인 작업은 `cancelled` flag와 단계별 체크포인트로 중단하고, 취소 시 VLM unload, 임시 파일 삭제, 상태 map 정리, dedup hash 제거를 수행했다.

활용 가능한 표현:

> 장시간 실행되는 PDF/VLM 번역 작업에 취소 체크포인트와 정리 로직을 추가해, 취소 이후에도 모델 추론과 임시 파일이 남는 문제를 방지했습니다.

### 다중 PDF 배치 업로드와 VLM 재사용

여러 PDF를 업로드할 때 파일마다 VLM을 로드하고 언로드하면 모델 초기화 비용이 반복될 수 있었다.  
이를 줄이기 위해 다중 PDF 배치 API를 추가하고, 배치 처리 중 마지막 파일이 아니면 VLM unload를 보류해 다음 파일에서 재사용하도록 구성했다.

예외가 발생해도 마지막에는 unload가 보장되도록 `finally` 기반 정리 구조를 적용했다.

활용 가능한 표현:

> 다중 PDF 업로드 시 파일별로 VLM을 반복 로드하던 비용을 줄이기 위해 배치 처리 구조를 도입하고, 처리 중 모델을 재사용하도록 개선했습니다.

### 슬라이드 번역 진행률/ETA 안정화

PDF 슬라이드 번역은 OCR, VLM 번역, PDF 생성처럼 시간이 오래 걸리는 단계로 구성되어 있었다.  
사용자가 현재 어떤 단계가 진행 중인지, 얼마나 기다려야 하는지 알기 어려웠기 때문에 단계 기반 상태 모델을 확장했다.

상태 정보에 `stage`, `stage_current`, `stage_total`, `stage_started_at`, `last_page_at`, `avg_page_duration`을 추가하고, OCR과 번역 단계별 평균 처리 시간을 분리해 ETA를 계산했다.

활용 가능한 표현:

> OCR, VLM 번역, PDF 생성 단계를 분리한 상태 모델을 설계하고, 단계별 진행률과 ETA를 제공해 장시간 작업의 사용자 대기 경험을 개선했습니다.

### WebSocket 재연결 루프 방지

강의 종료나 화면 이동처럼 의도적으로 WebSocket을 닫는 상황에서도 자동 재연결 로직이 실행될 수 있었다.  
이를 방지하기 위해 의도적 종료와 비정상 종료를 구분하는 flag를 추가하고, 의도적 종료 시에는 재연결을 수행하지 않도록 수정했다.

활용 가능한 표현:

> WebSocket 종료 상황을 의도적 종료와 비정상 종료로 구분해, 화면 이동 후 자동 재연결이 반복되는 문제를 방지했습니다.

---

# 4. 프로젝트별 추천 활용 문항

## 다행

- 협업 경험
- 높은 목표 달성 경험
- 데이터 파이프라인/배치 안정성 중심 직무 경험

추천 소재:

- 항공권 수집 배치 실패 구간 재처리 구조 개선
- 항공권 데이터 기준/API 응답/알림 조건 협업
- 항공권 알림 API 및 배치 구현

## HearBe

- 직무 강점 및 관련 경험
- 인증/인가, Redis, 사용자 상태 관리 중심 경험
- 협업 경험 중 화면 공유 역할 분리 사례

추천 소재:

- Spring Security + JWT 인증/인가
- Redis Refresh Token + HttpOnly Cookie
- Redis 공유 코드 API와 화면 공유 진입 흐름

## 번역의 민족

- 높은 목표 달성 경험
- AI 처리 안정화 경험
- 사용자 경험 개선 중심 트러블슈팅

추천 소재:

- ASR/MT 품질 게이트
- PDF/VLM 번역 작업 취소와 리소스 정리
- ASR 문장 분할과 WebSocket 순차 송출

---

# 5. 프로젝트별 한 줄 요약

## 다행

> 외부 항공권 데이터를 수집하고 HDFS/Spark ETL을 거쳐 서비스 API와 알림 배치에 연결하며, 데이터 파이프라인의 정합성과 운영 안정성을 개선한 프로젝트.

## HearBe

> Spring Security/JWT 인증, Redis Refresh Token, 민감정보 암호화, 주문 도메인, 공유 코드 API를 구현하며 사용자 상태와 인증 흐름을 안정적으로 관리한 프로젝트.

## 번역의 민족

> ASR/MT/VLM 기반 AI 처리 결과가 사용자에게 안정적으로 전달되도록 품질 게이트, WebSocket 실시간 송출, 장시간 작업 취소와 상태 관리를 구현한 프로젝트.

